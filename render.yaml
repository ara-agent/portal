services:
  # ─── Node.js signalling + key server ────────────────────────────────────────
  - type: web
    name: portal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /ice-config
    envVars:
      - key: PORTAL_KEYS_ENABLED
        value: "true"
      - key: SOLANA_RPC_URL
        sync: false       # fill in Render dashboard
      - key: PROGRAM_ID
        sync: false
      - key: TREASURY_PUBKEY
        sync: false
      - key: ADMIN_KEY
        generateValue: true
      - key: ADMIN_SECRET
        generateValue: true
      - key: ADMIN_KEYS
        sync: false       # comma-separated list of permanent portal keys (survive redeploys)
      - key: FACE_SERVICE_URL
        sync: false       # set to the face service URL after it deploys
      - key: FACE_SERVICE_TOKEN
        fromService:      # shared secret — must match the face service
          type: web
          name: portal-face
          envVarKey: FACE_SERVICE_TOKEN
      - key: TURN_URL_UDP
        sync: false
      - key: TURN_URL_TCP
        sync: false
      - key: TURN_USERNAME
        sync: false
      - key: TURN_CREDENTIAL
        sync: false

  # ─── Python face recognition service ────────────────────────────────────────
  - type: web
    name: portal-face
    runtime: python
    plan: starter
    rootDir: face_service
    buildCommand: pip install -r requirements.txt && python download_models.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1
    healthCheckPath: /health
    disk:
      name: face-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: FACE_DATA_FILE
        value: /data/encodings.json
      - key: FACE_SERVICE_TOKEN
        generateValue: true   # shared secret required on all face-service calls
      - key: FACE_CORS_ORIGINS
        sync: false           # optional comma-separated portal origin(s); empty = no browser CORS
